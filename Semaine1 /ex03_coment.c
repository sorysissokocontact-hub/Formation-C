/* Ci-dessous nous retrouvons les directives préprocesseur ce sont des fichiers communément appelés bibliothèques et qui contiennent énormément de fonctions préenregistrés comme par exemple la fonction printf */
#include <stdlib.h>
#include <stdio.h>

/* ici nous avons la fonction principale main c'est par cette fonction que commence tout les programmes.*/
int main(int argc , const char * argv[]) {
    printf("Hello, World!\nBonjour Sory!\n");// ici nous retrouvons la fonction printf qui nous sert a afficher des messages à l'écran et dans la console
    return 0;// ceci est l'instruction return qui nous informe de la fin du programme et qu'il s'est exécuté sans problème 
    }
